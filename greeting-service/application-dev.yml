spring:
  application:
    name: greeting-service
management:
  endpoints:
    web:
      exposure:
        include:
          - '*'
greeting-service: #criando propriedade customizada.
  greeting: "Hello"
  default-value: "World"    